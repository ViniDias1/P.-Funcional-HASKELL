--Calcula a area do triangulo pela fórmula da (Base*Altura)/2

triangulo b h = (b*h)/2

--Calcula a area do triângulo pela fórmula de HERON

areaTriangulo a b c = sqrt(s * (s-a) * (s-b) * (s-c))
 where
  s = (a + b + c)/2
  